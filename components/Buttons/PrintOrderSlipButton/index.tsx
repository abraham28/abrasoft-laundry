import React from "react";
import { Button } from "react-bootstrap";

const PrintOrderSlipButton = () => {
  return <Button variant="primary">Print Order Slip</Button>;
};

export default PrintOrderSlipButton;
