import React from "react";
import { FormControl } from "react-bootstrap";

const SearchBar = () => {
  return (
    <div>
      <FormControl type="search" placeholder="Type anything here" />
    </div>
  );
};

export default SearchBar;
